// src/config.js
export const CONFIG = {
  // Change this to your actual backend URL when deployed:
  API_BASE_URL: 'https://your-real-api.com', // ← 🎯 ONLY LINE TO CHANGE!
  
  // Optional: Increase timeout for production
  API_TIMEOUT: 10000,
  
  // Change to 'production' when deploying:
  ENVIRONMENT: 'production'
};

export default CONFIG;